# Copilot Instructions

## Project Guidelines
- User prefers keeping TreatWarningsAsErrors enabled and handling exceptions for generated code only.
- Preserve callback lifetime semantics when fixing leaks in ReactiveExtensions to prevent UI issues caused by weak callbacks being collected too early.